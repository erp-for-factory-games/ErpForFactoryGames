# Build output
bin/
obj/
out/
publish/
artifacts/
.nuke/temp/
*.user
*.suo

# Claude Code — local-only files (shared settings.json IS committed)
.claude/settings.local.json
.claude/.credentials*
.claude/scheduled_tasks.lock
# Claude Code agent worktrees (transient, never commit)
.claude/worktrees/
.claude/scheduled_tasks.lock

# Rider / VS / VS Code
.idea/
.vs/
.vscode/

# Aspire
**/.aspire/

# NCrunch / Test runners
*.ncrunchsolution
*.ncrunchproject
TestResults/
coverage*.json
coverage*.xml
*.coverage
*.coveragexml

# Logs
*.log

# OS
Thumbs.db
.DS_Store

# User secrets / local settings
*.user.props
*.local.json
secrets.json

# Tooling
.tools/

# Node
node_modules/

# Playwright MCP working artifacts (screenshots, console logs, snapshots)
.playwright-mcp/
test/ui-tests/

# Local reference assets (user-only, not for redistribution)
#.assets/ # TODO: Handle this differently. Also need to tidy up a bit, this folder turned a bit messy

# SQLite plan storage (ADR-0018) — runtime data, never committed
*.db
*.db-shm
*.db-wal

# Keycloak persisted DB (AppHost WithDataBindMount, #303) — local-dev state,
# holds brokered identities across restarts; never committed
keycloak-data/

# Planner scratch snapshot (Steam ID + local save path inside) — dev-only
factory_state.json


*.lscache